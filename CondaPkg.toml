[deps]
python = ">=3.10,<4"

[pip.deps]
# The Triton compiler is a compile-time-only dependency: TTIR text goes in,
# cubin + launch metadata come out. Pinned exactly — TTIR is not a stable
# format across Triton versions.
triton = "==3.7.1"
